import React from 'react'
import { UpdateProfileContainer } from './style'

export default function UpdateProfile({ children }) {
    return <UpdateProfileContainer>{children}</UpdateProfileContainer>
}
